<nav class="navbar navbar-expand-md navbar-light border-bottom sticky-top">
  <div class="container-fluid px-4">
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/listings">
      <i class="fa-solid fa-compass"></i>
      <span class="fs-4 fw-bold text-danger d-none d-lg-block">wanderLust</span>
    </a>

    <!-- Toggle Button -->
    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="d-flex w-100 justify-content-between align-items-center flex-column flex-md-row mt-3 mt-md-0">

        <!-- Left Links (Optional, keeping Explore for standard look) -->
        <ul class="navbar-nav mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link" href="/listings">Explore</a>
          </li>
        </ul>

        <!-- Center Search Bar -->
        <div class="flex-grow-1 d-flex justify-content-center px-3 mb-3 mb-md-0 w-100">
          <form class="search-form" action="/listings" method="GET" role="search">
            <input class="form-control search-input" type="search" name="q"
              placeholder="Search destinations, title, location..." aria-label="Search"
              value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>" />
            <button class="btn-search" type="submit">
              <i class="fa-solid fa-magnifying-glass"></i>
            </button>
          </form>
        </div>

        <!-- Right Links -->
        <ul class="navbar-nav flex-row align-items-center gap-3">
          <li class="nav-item">
            <a class="nav-link fw-semibold text-nowrap" href="/listings/new">Airbnb home</a>
          </li>
          <% if(!currUser) { %>
            <li class="nav-item">
              <a class="nav-link fw-bold text-nowrap" href="/signup">Sign up</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-bold text-nowrap" href="/login">Log in</a>
            </li>
            <% } else { %>
              <div class="dropdown">
                <button class="btn btn-light rounded-pill border py-2 px-3 d-flex align-items-center gap-2 shadow-sm"
                  type="button" data-bs-toggle="dropdown" aria-expanded="false" style="transition: box-shadow 0.2s;">
                  <i class="fa-solid fa-bars"></i>
                  <% if(currUser.avatar && currUser.avatar.url) { %>
                    <img src="<%= currUser.avatar.url %>" class="rounded-circle" style="width: 30px; height: 30px; object-fit: cover;" alt="Profile Picture">
                  <% } else { %>
                    <div class="bg-secondary rounded-circle d-flex align-items-center justify-content-center text-white"
                      style="width: 30px; height: 30px;">
                      <i class="fa-solid fa-user"></i>
                    </div>
                  <% } %>
                </button>

                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-2 rounded-4 py-2">
                  <li><a class="dropdown-item fw-semibold" href="/profile/show">Profile</a></li>
                  <li><a class="dropdown-item" href="/favourite/show">Favourites</a></li>
                  <li><a class="dropdown-item" href="/booking/show">Trips</a></li>
                  <li>
                    <hr class="dropdown-divider">
                  </li>
                  <li><a class="dropdown-item" href="/listings/new">Airbnb your home</a></li>
                  <li>
                    <hr class="dropdown-divider">
                  </li>
                  <li>
                    <form action="/logout" method="get" class="m-0 p-0">
                      <button class="dropdown-item text-danger fw-semibold">Log out</button>
                    </form>
                  </li>
                </ul>
              </div>
              <% } %>
        </ul>

      </div>
    </div>
  </div>
</nav>